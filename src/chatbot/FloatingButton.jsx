import React from 'react'

function FloatingButton({ onClick }) {
  // TODO: Style and position the button
  return (
    <button onClick={onClick}>
      💬
    </button>
  )
}

export default FloatingButton